<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.edu.mju.ccce.dtrsystem.dao.LoginDao">

    <select id="selcetUser" parameterType="java.util.HashMap" resultType="java.util.HashMap">
        SELECT USER_NAME,USER_SEX,CLASS_ID
        FROM user
        WHERE USER_NAME =#{userName,jdbcType=VARCHAR} AND USER_PASS = #{userPass,jdbcType=VARCHAR} AND USER_STATUS = 0
    </select>

</mapper>